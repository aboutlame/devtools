# Mentee Power

[![Java CI with CheckStyle and JaCoCo](https://github.com/aboutlame/devtools/actions/workflows/ci.yml/badge.svg)](https://github.com/aboutlame/devtools/actions/workflows/ci.yml)

### Учебный Java-проект для демонстрации базовой структуры приложения, работы с пакетами и простой бизнес-логики.

#### Проект ориентирован на отработку навыков:
#### - 📦 организации кода
#### - 🧪 тестирования
#### - 📏 контроля качества
#### - 🔄 работы с Git и CI

---

## 🚀 Quick Start

Сборка и проверка проекта выполняются через **Gradle**.

### - Gradle Tool Window (IntelliJ IDEA)

- `build` — сборка проекта
- `test` — запуск тестов
- `checkstyleMain` — проверка кодстайла основного кода
- `checkstyleTest` — проверка кодстайла тестов
- `jacocoTestReport` — отчёт по покрытию кода
- `jacocoTestCoverageVerification` — проверка минимального уровня покрытия


### - Команды Gradle из терминала

#### ./gradlew build  
#### ./gradlew test  
#### ./gradlew checkstyleMain  
#### ./gradlew checkstyleTest  
#### ./gradlew jacocoTestReport  
#### ./gradlew jacocoTestCoverageVerification

---

## 📦 Package

ru.mentee.power

Используется для логической группировки кода и предотвращения конфликтов имён классов.

---

## 📁 Структура проекта

```text
devtools/
├── .github/workflows/
│   └── ci.yml              # CI-пайплайн GitHub Actions
├── src/
│   ├── main/java/ru/mentee/power/devtools/
│   │   ├── progress/       # Логика расчёта прогресса
│   │   └── student/        # Модели и работа с учениками
│   └── test/java/ru/mentee/power/devtools/
│       ├── progress/       # Тесты progress-модуля
│       └── student/        # Тесты student-модуля
├── build.gradle            # Конфигурация сборки Gradle
├── settings.gradle         # Настройки проекта
└── README.md               # Документация проекта
```

---

## 🛠 Технологии

Java · Gradle · JUnit · AssertJ · Checkstyle · JaCoCo · IntelliJ IDEA

---

## 🔁 Git локальный цикл: шаги и команды

1. **Проверка состояния**
   ```bash
   git status

2. **Создание рабочей ветки**
   ```bash
   git checkout -b feature/DVT-3
   ```

3. **Просмотр изменений**
   ```bash
   git diff
   ```

4. **Добавление файлов**
   ```bash
   git add .
   ```

5. **Коммит изменений**
   ```bash
   git commit -m "Описание изменений"
   ```

6. **История коммитов**
   ```bash
   git log --oneline
   ```

7. **Слияние с основной веткой**
   ```bash
   git checkout main
   git merge feature/DVT-3
   ```

8. **Удаление рабочей ветки**
   ```bash
   git branch -d feature/DVT-3
   ```

---

## 🧪 Сценарий ручной проверки DVT-6

### Запуск приложения
1. Откройте Gradle Tool Window (View → Tool Windows → Gradle)
2. Выполните: devtools → Tasks → application → run
3. Ожидаемый вывод в Run Tool Window:
   Суммарно: пройдено 25 из 36 уроков, осталось 11 уроков

### Запуск тестов
1. Откройте Gradle Tool Window
2. Выполните: devtools → Tasks → verification → test
3. Ожидаемый вывод: BUILD SUCCESSFUL, все тесты зелёные

### Отладка через Debug
1. Установите breakpoint на строке цикла while в ProgressTracker.calculateProgress
2. Запустите Debug: кликните правой кнопкой на main → Debug 'ProgressTracker.main()'
3. Используйте Step Over (F8) для прохождения итераций
4. Проверьте Variables: counter, remainingHours должны изменяться корректно
5. Используйте Evaluate Expression (Alt+F8): вычислите remainingLessons * 2
6. Ожидаемый результат Evaluate: 14 (для completedLessons=5, totalLessons=12)

### Что делать при ошибках
- Если вывод некорректен: проверьте логику цикла через Debug
- Если тесты красные: откройте вывод теста, найдите AssertionError, скорректируйте метод
- Если Debug не останавливается: убедитесь, что breakpoint установлен (красный кружок)

---

## 🎨 Кодстайл-гайд проекта devtools

Проект следует правилам Google Java Style Guide с адаптацией.
Автоматическая проверка: ./gradlew checkstyleMain

### 1. Именование методов: camelCase

До:    `public void add_student(Student s) { }`\
После: `public void addStudent(Student student)` { }

Почему: Java Convention требует camelCase для методов.
Источник: https://google.github.io/styleguide/javaguide.html#s5.3-camel-case

### 2. Пробелы после if/for/while

До:    `if(condition)` {\
После: `if (condition)` {

Почему: улучшает читаемость, отделяет ключевое слово от выражения.
Источник: Oracle Code Conventions — Whitespace

### 3. Длина строки: максимум 120 символов

До:    `public List getStudentsFromSpecificCityWithVeryLongName...`\
После: `public List getStudentsByCity(String city) {`

Почему: длинные строки затрудняют чтение в редакторе и при code review.
Источник: https://google.github.io/styleguide/javaguide.html#s4.4-column-limit

### 4. Порядок импортов

До:    `import java.util.List; import java.util.ArrayList; import java.io.File;`\
После: `import java.io.File; import java.util.ArrayList; import java.util.List;`

Почему: алфавитный порядок упрощает поиск импортов.
Источник: IntelliJ IDEA → Code → Optimize Imports

### 5. Фигурные скобки для if

До:    `if (condition) doSomething();`\
После: `if (condition) { doSomething(); }`

Почему: скобки обязательны даже для однострочных блоков.
Источник: https://google.github.io/styleguide/javaguide.html#s4.1.1-braces-always-used

---

## 🤖 CI-пайплайн настроен

В проекте настроен CI-пайплайн на базе **GitHub Actions**  
(файл: `.github/workflows/ci.yml`).

### Назначение пайплайна
CI автоматически проверяет качество кода и корректность сборки при каждом изменении в репозитории.

### Когда запускается
- при `push` в ветки:
    - `main`
    - `feature/**`
- при создании `pull request` в ветку `main`

### Основные шаги пайплайна
1. Клонирование репозитория
2. Подготовка Gradle Wrapper
3. Установка **JDK 25 (Temurin)**
4. Проверка кодстайла (`checkstyleMain`)
5. Запуск тестов (`test`)
6. Генерация отчёта покрытия (`jacocoTestReport`)
7. Проверка минимального покрытия кода (`jacocoTestCoverageVerification`)
8. Сборка проекта (`build`)

### Цель
- гарантировать, что код:
    - компилируется
    - соответствует кодстайлу
    - проходит тесты
    - имеет достаточное покрытие
- предотвратить попадание некорректного кода в `main`

---

## ✅ Code Review Checklist

Используйте этот чеклист для само-ревью перед запросом ревью у ментора:

### Функциональность
- [ ] Код решает поставленную задачу полностью
- [ ] Обработаны граничные случаи (null, пустые данные, экстремальные значения)
- [ ] Обработка ошибок реализована корректно

### Тесты
- [ ] Добавлены тесты для нового функционала (или обновлены существующие)
- [ ] Все тесты проходят локально: `./gradlew test`
- [ ] Покрыты позитивные и негативные сценарии
- [ ] JaCoCo coverage ≥ 80% для нового кода

### Читаемость и стиль
- [ ] Имена переменных, методов и классов отражают назначение
- [ ] Нет дублирования кода (DRY principle)
- [ ] Checkstyle проходит без ошибок: `./gradlew checkstyleMain`
- [ ] Нет закомментированного кода или отладочного вывода (`System.out.println`)

### Документация
- [ ] README обновлён (если добавлена новая функциональность)
- [ ] Публичные методы имеют JavaDoc (если применимо)
- [ ] Примеры использования актуальны
- [ ] Runbook обновлён (если изменились команды запуска/проверки)

### Производительность и безопасность
- [ ] Нет очевидных проблем производительности
- [ ] Нет хардкода паролей, токенов или конфиденциальных данных

---

## 💬 Примеры Code Review комментариев

### Хорошие комментарии (конструктивные)

**Пример 1:**

**Проблема:** Метод `calculateDiscount` (строка 45) имеет 3 вложенных if-else и 40 строк.
**Почему это важно:** Сложная логика плохо тестируется и тяжело поддерживается.
**Предложение:** Вынести каждое условие в отдельный метод (например, `isEligibleForBonusDiscount()`)
и использовать паттерн Strategy для разных типов скидок.


**Пример 2:**

**Проблема:** Тест `testProcessOrder` (строка 78) проверяет только успешный сценарий.
**Почему это важно:** Не проверена обработка ошибок при недостаточном балансе.
**Предложение:** Добавить тест `testProcessOrder_InsufficientBalance_ThrowsException()`
с использованием `assertThatThrownBy()`.


### Плохие комментарии (неконструктивные)

**Пример 1:**

Этот код ужасен, полностью переписать.


**Почему плохо:** Нет конкретики (что именно плохо), нет предложения (как исправить),
токсичный тон (демотивирует автора).

**Пример 2:**

Здесь лучше использовать Stream API.


**Почему плохо:** Нет объяснения почему лучше, нет примера как переписать,
неясно какую проблему это решает.

---

## 🧪 Результаты само-ревью DVT-9

### 1. README не отражал актуальную структуру проекта
**Файл:** README.md

В первоначальной версии README отсутствовало описание фактической структуры проекта  
(`src/main`, `src/test`, разбиение по пакетам `progress` и `student`),  
из-за чего было сложно понять архитектуру без изучения исходного кода.

**Исправление:**  
Добавлен отдельный раздел **«📁 Структура проекта»** с описанием пакетов и их назначения.


### 2. В README присутствовала неотформатированная строка
**Файл:** README.md

В документе присутствовал артефакт вида `yaml Copy code`,  
оставшийся после копирования примеров, что нарушало форматирование Markdown  
и ухудшало читаемость документа.

**Исправление:**  
Удалены некорректные строки, Markdown-разметка приведена в аккуратный и читаемый вид.


### 3. README имел неструктурированную и перегруженную подачу
**Файл:** README.md

Разделы README были расположены нелогично, часть информации дублировалась,  
а ключевые блоки (Gradle, CI, качество кода) не были явно сгруппированы.

**Исправление:**  
README переработан:
- разделы логически сгруппированы
- добавлены заголовки второго и третьего уровня
- улучшена навигация и читаемость документа

## 🔎 Информационный поиск — результаты DVT-11

---

### Запросы и источники

| № | Запрос              | Операторы                                                                 | Официальный источник |
|---|---------------------|---------------------------------------------------------------------------|----------------------|
| 1 | Lombok Gradle Short | `site:search.maven.org "lombok" OR site:mvnrepository.com "lombok"`       | https://central.sonatype.com/artifact/org.projectlombok/lombok |
| 2 | Java Stream API     | `site:docs.oracle.com "stream api" intitle:api java 25`                   | https://docs.oracle.com/en/middleware/standalone/coherence/applications/using-repository-api.html#GUID-6FE2A09E-972B95BA87D6 |
| 3 | IntelliJ IDEA Keymap| `site:www.jetbrains.com/help "keymap" "intellij idea"`                    | https://www.jetbrains.com/help/idea/settings-keymap.html |

## 🧠 AI-промпты и сравнение ассистентов

### Промпт 1: План решения

Нужно добавить валидацию email в record Student  
Контекст: Java 25, у меня есть record Student, добавить 3 поле с валидацией, проект на Gradle  
Ограничения: без RegEx, простая проверка с `@` и `.`  
Ожидаемый результат: план из 3–5 шагов, объяснение «почему именно так», **НЕ пиши код — я реализую сам**  
Критерии успеха: record `Student` объявлен и имеет валидацию `email`

---

## 🐳 AI-1 (DeepSeek)

Отличный контекст — это как раз «правильная» задача для понимания `record` и инвариантов.  
Ниже — чёткий план без кода, с объяснением почему именно так, и привязкой к **Java 25 + Gradle**.

#### Цель

Добавить 3-е поле `email` в `record Student` и обеспечить валидацию при создании объекта, без RegEx, с простой логикой (`@` и `.`).

#### План (4 шага)

##### Шаг 1. Расширить контракт Student

**Что сделать**

Добавить третье поле `email` в объявление `record`.

**Почему так**

- `record` — это value object, его контракт задаётся в заголовке
- Все поля `record` — часть неизменяемого состояния, значит `email` должен быть проверен один раз при создании, а не «где-то потом»

---

##### Шаг 2. Использовать компактный конструктор record

**Что сделать**

Добавить compact constructor у `record`.  
В нём выполнить проверку `email`.

**Почему так**

Compact-конструктор:
- автоматически получает все параметры `record`
- вызывается до создания объекта

Это каноничный способ валидации инвариантов в `record`.

---

##### Шаг 3. Реализовать простую валидацию без RegEx

**Что проверить логически (минимум)**

- `email` не `null`
- Содержит ровно один символ `@`
- Символ `.` находится после `@`
- `@` не первый и не последний символ
- `.` не последний символ

**Почему так**

- Явное ограничение: без RegEx
- Проверки:
    - покрывают большинство ошибок
    - легко читаются
    - не превращают код в магию

---

##### Шаг 4. Явно выбросить исключение

**Что сделать**

При невалидном `email` выбрасывать `IllegalArgumentException`.

**Почему так**

- стандарт для проверки аргументов
- хорошо читается в тестах
- предотвращает создание некорректного объекта

В `record` лучше падать сразу, чем хранить битые данные.

---

#### Критерии успеха

- `Student` объявлен как `record`
- У `record` есть поле `email`
- Валидация происходит при создании объекта
- Некорректный email → объект не создаётся
- RegEx не используется

---

## 🤖 AI-2 (ChatGPT)

Отлично, вот пошаговый план с объяснениями для добавления валидации email в record `Student` в проекте на **Java 25 с Gradle**.

### План из 4 шагов

#### 1. Добавить поле email в объявление record Student и кастомизировать его конструктор

**Почему:**  
`record` является неизменяемым (immutable), поэтому вся валидация должна происходить в момент его создания — в конструкторе.  
Необходимо объявить канонический или компактный конструктор и разместить в нём логику проверки.

---

#### 2. Добавить проверку email на null и базовое форматирование

**Почему:**  
Это фундаментальные проверки целостности данных.

Проверяется, что:
- `email` не `null`
- строка содержит `@`, который не первый и не последний
- строка содержит `.` после `@`

**Принцип:**  
Лучше отфильтровать некорректные данные сразу, чем допустить создание объекта с заведомо ошибочным email.

---

#### 3. При невалидном email выбрасывать исключение

**Почему:**  
`IllegalArgumentException` — стандартный и понятный способ сигнализировать об ошибке аргумента.  
Исключение предотвращает создание невалидного объекта и защищает систему от распространения ошибочных данных.

---

#### 4. Обновить тесты или код создания Student

**Почему:**  
После добавления валидации существующий код может начать выбрасывать исключения.

Необходимо:
- добавить unit-тесты на валидные и невалидные email
- либо убедиться, что вызывающий код корректно обрабатывает ошибку

---

### Критерии успеха

- `Student` объявлен с тремя полями
- Конструктор гарантирует валидность email
- Любой существующий объект `Student` содержит корректный email

---

## 🧪 Шаг 7. Промпт 2 — проверка кода

### Код для проверки

```java
package ru.mentee.power;

public record MenteeProgress(String menteeName, int sprintNumber, int plannedHoursPerWeek) {

  public boolean readyForSprint() {
    return plannedHoursPerWeek >= 3;
  }

  public String summary() {
    return "Sprint %d → %s: planned %d h".formatted(
        sprintNumber,
        menteeName,
        plannedHoursPerWeek
    );
  }
}
```

## Что проверить

- Логические ошибки  
- Code Conventions  
- Производительность  

---

## 🐳 AI-1 (DeepSeek)

### Анализ кода

#### Логические ошибки
Метод `readyForSprint()` реализован корректно и содержит понятное бизнес-правило. Однако значение `3` является магическим числом — без контекста неясно, почему именно 3 часа считаются минимальным порогом. Для лучшей читаемости стоит вынести это значение в константу.

Метод `summary()` логически корректен, но не учитывает возможность некорректных входных данных, например `menteeName == null` или отрицательные значения `plannedHoursPerWeek`.

#### Code Conventions
Именование соответствует Java Conventions. Использование `record` оправдано, так как класс является value object. Форматирование строки с использованием `String.formatted()` выглядит современно и читаемо.

Отсутствует JavaDoc — для публичного API это может быть минусом.

#### Производительность
Код не содержит проблем с производительностью. Все операции простые и выполняются за O(1). Использование `formatted()` допустимо, так как метод `summary()` не предполагается к вызову в горячем цикле.

---

## 🤖 AI-2 (ChatGPT)

### Анализ кода

#### Логические ошибки
Логика методов корректна, но отсутствует валидация входных данных. `plannedHoursPerWeek` может быть отрицательным, а `sprintNumber` — нулевым или отрицательным, что может привести к некорректному состоянию объекта. Для `record` такие проверки желательно выполнять в компактном конструкторе.

Метод `readyForSprint()` использует жёстко заданный порог `>= 3`, который не документирован и может быть непонятен при чтении кода.

#### Code Conventions
Код аккуратный, читаемый и соответствует стандартам Java. Использование `record` полностью оправдано. Методы названы ясно и отражают своё назначение.

Можно улучшить читаемость, вынеся пороговое значение часов в `private static final` константу и добавив краткий JavaDoc к публичным методам.

#### Производительность
Производительность не вызывает вопросов. Все методы выполняются быстро и не создают лишних объектов. Использование `String.formatted()` является допустимым и не создаёт проблем в данном контексте.

---

## 🔍 Сравнение

Оба ассистента указали на использование магического числа `3` и отсутствие валидации входных данных.  
DeepSeek сделал акцент на отсутствии JavaDoc и общей архитектурной аккуратности.  
ChatGPT более явно подчеркнул необходимость проверок именно в конструкторе `record`, что важно с точки зрения инвариантов.  
Наиболее полезным оказалось замечание о валидации данных при создании объекта, так как оно напрямую связано с корректным использованием `record`.

---

## DVT-12: Личный глоссарий терминов Dev Tools

---

## 🎯 Цель

Создать персональный глоссарий технических терминов Dev Tools с официальными источниками для точного использования терминологии в документации, code review и командной коммуникации.

---

## 📚 Личный глоссарий терминов Dev Tools

### Категория: Java-экосистема

### JDK — Java Development Kit
**Определение:** Development environment for building Java applications, including compiler (javac), tools, and runtime.  
**Контекст использования:** Используется для компиляции и сборки Java-проектов.  
**Пример:** Для сборки проекта Gradle использует установленный JDK 25.  
**Источник:** https://docs.oracle.com/en/java/javase/21/docs/

---

### JRE — Java Runtime Environment
**Определение:** Environment required to run Java applications, including JVM and standard libraries.  
**Контекст использования:** Нужен только для запуска Java-приложений без компиляции.  
**Пример:** Запуск `.jar` файла на сервере без инструментов разработки.  
**Источник:** https://docs.oracle.com/javase/8/docs/technotes/guides/

---

### JVM — Java Virtual Machine
**Определение:** Virtual machine that executes Java bytecode and provides platform independence.  
**Контекст использования:** Выполняет байт-код, сгенерированный компилятором Java.  
**Пример:** JVM запускается при выполнении команды `java Main`.  
**Источник:** https://docs.oracle.com/javase/specs/

---

### Dependency — Зависимость
**Определение:** External library required by a project to compile or run.  
**Контекст использования:** Подключается через Gradle или Maven.  
**Пример:** Lombok подключён как dependency в `build.gradle`.  
**Источник:** https://docs.gradle.org/current/userguide/dependency_management.html

---

### Gradle Wrapper — Gradle Wrapper
**Определение:** Script that allows running Gradle with a predefined version without global installation.  
**Контекст использования:** Гарантирует одинаковую версию Gradle у всей команды.  
**Пример:** Команда `./gradlew build`.  
**Источник:** https://docs.gradle.org/current/userguide/gradle_wrapper.html

---

## Категория: Инструменты разработки

### IDE — Integrated Development Environment
**Определение:** Software application that provides tools for software development.  
**Контекст использования:** Используется для написания, отладки и запуска кода.  
**Пример:** IntelliJ IDEA используется для разработки Java-проекта.  
**Источник:** https://www.jetbrains.com/help/idea/

---

### Git — Git
**Определение:** Distributed version control system for tracking changes in source code.  
**Контекст использования:** Используется для управления историей изменений проекта.  
**Пример:** `git commit -m "feat: add glossary"`.  
**Источник:** https://git-scm.com/docs

---

### Repository — Репозиторий
**Определение:** Storage location for project files and revision history.  
**Контекст использования:** Хранит код и историю изменений.  
**Пример:** Репозиторий проекта размещён на GitHub.  
**Источник:** https://git-scm.com/docs/gitglossary

---

### Commit — Коммит
**Определение:** Snapshot of changes recorded in a repository.  
**Контекст использования:** Фиксирует логически завершённые изменения.  
**Пример:** Каждый шаг задания фиксируется отдельным коммитом.  
**Источник:** https://git-scm.com/docs/git-commit

---

### Branch — Ветка
**Определение:** Movable pointer to a sequence of commits.  
**Контекст использования:** Используется для изоляции разработки.  
**Пример:** Работа ведётся в ветке `feature/DVT-12`.  
**Источник:** https://git-scm.com/docs/git-branch

---

### Pull Request — Pull Request
**Определение:** Request to merge changes from one branch into another.  
**Контекст использования:** Используется для code review перед слиянием.  
**Пример:** PR из `feature/DVT-12` в `master`.  
**Источник:** https://docs.github.com/en/pull-requests

---

### Checkstyle — Checkstyle
**Определение:** Static code analysis tool for checking Java code style.  
**Контекст использования:** Проверяет соответствие стилю кодирования.  
**Пример:** Проверка через `./gradlew checkstyleMain`.  
**Источник:** https://checkstyle.org/

---

### Debug — Отладка
**Определение:** Process of identifying and fixing defects in software.  
**Контекст использования:** Используется при анализе логики работы программы.  
**Пример:** Запуск приложения в Debug режиме в IDE.  
**Источник:** https://www.jetbrains.com/help/idea/debugging-code.html

---

### Breakpoint — Точка останова
**Определение:** Marker that pauses program execution during debugging.  
**Контекст использования:** Позволяет проверить состояние программы.  
**Пример:** Breakpoint на строке метода `main`.  
**Источник:** https://www.jetbrains.com/help/idea/using-breakpoints.html

---

## Категория: Процессы и практики

### Code Review — Code Review
**Определение:** Process of reviewing code changes by peers.  
**Контекст использования:** Повышает качество и читаемость кода.  
**Пример:** Проверка PR перед merge.  
**Источник:** https://martinfowler.com/articles/code-review/

---

### CI/CD — CI/CD
**Определение:** Practices for continuous integration and deployment.  
**Контекст использования:** Автоматическая проверка и доставка кода.  
**Пример:** GitHub Actions запускает тесты на каждый PR.  
**Источник:** https://www.redhat.com/en/topics/devops/what-is-ci-cd

---

## ❓ Вопросы по сложным терминам

### Вопрос 1: JDK vs JRE vs JVM
**Задача:** Понять различие между JDK, JRE и JVM.  
**Контекст:** Изучены Oracle Docs, но роли компонентов путаются.  
**Ограничения:** Используется Java 25.  
**Ожидаемый результат:** Чёткое понимание назначения каждого компонента.  
**Критерии успеха:** Могу объяснить разницу и привести пример использования.

---

### Вопрос 2: Как работает Pull Request
**Задача:** Понять механизм Pull Request в GitHub.  
**Контекст:** Используется PR в учебном проекте.  
**Ограничения:** Нет опыта работы в команде.  
**Ожидаемый результат:** Понимание связи веток, коммитов и merge.  
**Критерии успеха:** Могу создать и корректно описать PR.
